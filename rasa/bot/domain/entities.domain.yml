version: "3.1"

entities:
  # name of a user interacting with the bot;
  # name of a person a bot is asked about;
  - name 
  - personal_question
  # - user_name # name of a user interacting with the bot
